// SPDX-License-Identifier: MIT
// scripts/compile_solc.js
//
// STUB -- Phase 3 (Local tooling)
//
// Full implementation lands in Phase 3. This will compile contracts/*.sol
// with the solc-js @ 0.8.26 pinned in package.json, using evmVersion=cancun
// and optimizer runs=200, then write artifacts (ABI + bytecode) to
// ./artifacts/ for the test runner and deploy scripts to consume.
//
// Kept as a stub in Phase 0 so `npm run compile` fails loudly rather than
// silently succeeding on an empty pipeline.

'use strict';

console.error('[compile_solc] Not implemented yet -- lands in Phase 3.');
process.exit(1);
